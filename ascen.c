#include <stdio.h>
int main()
{
int arr max_n;
    int i, max, min, n;
    printf("Enter size of the array: ");
    scanf("%d", &n);
    printf("Enter elements in the array: ");
    for(i=0; i<n; i++)
    {
        scanf("%d", &arr[i]);
 }
    
    for(i=1; i<n; i++)
    {

        min==arr[0];
  
        if(arr[i] > min)
        {
           
 
    printf("Maximum element = %d\n", i);
    

    return 0;
}
